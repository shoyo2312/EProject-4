config.stopBubbling = true
# Lets @Value sit on a final field under @RequiredArgsConstructor: Lombok copies it onto the
# generated constructor parameter, so configuration is constructor-injected like everything else
# instead of written into a mutable field by reflection after construction.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Value
